## 概要
Closes #544

- `--out` で証跡ファイルを書いていた EvalSeal 呼び出しは PR #549（issue #507）で既に撤去済みで読み手が消滅していたため、`dev-flow.js` から `--out` 指定を撤去
- `danger-grep` / `danger-grep-final` の `agentType` を書き込み可の `dev-runner-haiku` から read-only 専任の `dev-runner-haiku-ro` に戻す
- Security floor の決定論スクリプト4連発（`danger-grep` / `realized-diff` / `structural-classify` / `diff-hash-secfloor`）を `secfloor-classify.sh` 1本に統合し、`{hits, testsurf, files, format_only, hash}` を1JSONで返すように変更（-3 call、全 shape で削減）
- 統合後も read-only のまま実行可能（`--out` 撤去により least-privilege の後退なし）

## 動機

構造監査で 1 run あたりの subagent 呼び出しを数え上げた結果、判断系 agent（dev-planner / plan-reviewer / implementer / evaluator / pr-reviewer）は全て gate 直結で削減余地がほぼ無い一方、exec-proxy 側には統合余地があった。当初 4 件の統合案があったが `1 issue = 1 PR` の不変条件により、fail-closed な security gate の変更と低リスクな整理を分離する必要があり、本 issue は security floor の統合に絞った（低リスクな残り3件は #550 へ分離）。

## 変更内容

| ファイル | 変更内容 |
|---------|----------|
| `.claude/workflows/dev-flow.js` | `--out` 撤去、`danger-grep`/`danger-grep-final` の agentType を `-ro` に変更、security floor 4呼び出しを `secfloor-classify.sh` 呼び出し1本に統合 |
| `_shared/scripts/secfloor-classify.sh` | 新規。`danger-grep`/`realized-diff`/`structural-classify`/`diff-hash-secfloor` を統合した決定論スクリプト |
| `_shared/scripts/secfloor-classify.bats` | 新規。統合スクリプトのユニットテスト |
| `_lib/secfloor-unified.mjs` | 新規。統合 proxy 結果からフィールド別失敗ポリシーで SEC seed / merge tier 判定へ変換するロジック |
| `_lib/secfloor-unified.test.mjs` | 新規。フィールド別失敗ポリシー（hits=fail-closed / testsurf=既存据え置き / files=fail-safe / format_only・hash=fail-open）のユニットテスト |
| `_lib/secfloor-unified-routing.test.mjs` | 新規。統合 proxy が `null`/`ok:false`/schema不一致で返った場合に SEC seed が全て unchecked となり merge tier が HOLD になることを pin するルーティングテスト |
| `_lib/*-routing.test.mjs`（既存分） | 統合後の exec-proxy 呼び出し構成に合わせてテストを更新 |
| `.claude/rules/dev-flow.md` | exec-proxy 構成の変更をドキュメントに反映 |
| `_shared/scripts/diff-risk-classify.sh` / `.bats` | `--out` 撤去に伴う調整 |

## テスト計画

- [x] `tests/run-node-tests.sh` green
- [x] `bash tests/run-all-bats.sh --strict` green
- [x] `tools/sync-inlines.mjs --check` exit 0
- [x] 軸A invariant（security floor の blocking 判定）非抵触をテストで pin
