---
title: positive fixture - Claude Code hook lifecycle (BC2/BC5 + lifecycle signal)
---

# Hook lifecycle の挙動主張

`PreToolUse` を実行すると hook が呼ばれます。

`PostToolUse` では cleanup hook が呼ばれます。

SessionStart では init script が起動します。
