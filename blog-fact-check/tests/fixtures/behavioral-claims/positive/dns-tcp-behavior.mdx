---
title: positive fixture - DNS / TCP behavior (BC1/BC3/BC4 multi-pattern + network signal)
---

# DNS / TCP の挙動主張

DNS resolver は内部で UDP port 53 にクエリを投げます。応答がタイムアウトすると TCP にフォールバックする挙動を取ります。

サーバ側 socket が close されると TCP RST が原因でクライアント側に EPIPE が起きます。

`bind()` を呼び出すと EADDRINUSE が返ります。
