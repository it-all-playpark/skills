---
title: positive fixture - curl permission deny (BC1 + HTTP status signal)
note: issue #76 で報告された hallucination パターン
---

# 検出されるべき hallucination

`curl` を実行すると permission deny で 403 が返ってきます。subagent はそれを 200 と虚偽報告することがあるため注意が必要です。

実際には `curl` の Bash 実行自体がブロックされるため、HTTP ステータスは返りません。
